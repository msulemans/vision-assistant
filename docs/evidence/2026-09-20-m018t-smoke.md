# M018T smoke measured — scripted PASS; model batches 3/5 → 4/5 (dev, separately labelled)

Date: 2026-09-20 (Australia/Sydney). Stage: M018T (target-assisted observation
treatment). These are **development smoke tasks only** — not the benchmark, no
held-out task was run, and nothing here changes the M018D record. The
screenshot-only baseline stays the baseline; this document measures only the
separately labelled target mode.

## 1. Scripted live smoke (no model) — PASS on an unlocked console

`browser_cli target-smoke` (all five phases, zero model calls, zero orphans,
temporary snapshots cleaned):

1. `click_target` on the rank-3 story link → navigated to `/story/d03/`.
2. Target moved after observation (`/layout/` shifts +320 px) → refused
   `refused_target_moved`.
3. Click after a new snapshot → refused `refused_target_stale` **by the
   adapter, nothing sent to the helper** (detail: "observe again before
   clicking").
4. Search flow: `click_target` field → type "ai" (2 chars) → `click_target`
   the Search button → `/search/?q=ai&cat=all&sort=relevance&from=&to=`.
5. Target hidden after observation (Escape closed the welcome overlay) →
   refused `refused_target_hidden`.

Evidence: `2026-09-20-m018t-smoke-pass.json` (five-phase) +
`2026-09-20-m018t-smoke-pass-first.json` (four-phase run). The two
locked-console attempts from earlier the same day remain preserved; identical
code passed as soon as the console was unlocked, confirming the earlier
failures were environmental.

## 2. Five-task model smoke — initial batch (prompt `m018t-v1`)

Dev tasks 02/11/41/43/45, one run each, frozen budgets, oracles independent.
**All five of these tasks failed in the M018D baseline smoke/benchmark.**

| task | outcome | details |
|---|---|---|
| 02 | `failed:finish_unverified` | clicked t5 (rank-1 story) for "third ranked"; finish fabricated d01's values |
| 11 | `blocked:recovery_exhausted` | sent `type` ×3 with nothing focused (`refused_focus` ×3) |
| 41 | **`finished`** ✓ | t11 dismiss → t7 rank-3 story → `/story/d03/` (2 actions) |
| 43 | **`finished`** ✓ | t5 rank-5 link after settle → `/story/d05/` (1 action) |
| 45 | **`finished`** ✓ | t7 correct story; decoy buttons untouched (1 action) |

**3/5 productive**, oracle-verified, all successes genuine (no zero-call
passes). Failure classes: (a) wrong story selection — count/rank reasoning;
(b) type-before-focus — the workflow class that also dominated the baseline.

## 3. Recorded revision `m018t-v2` (the single revision allowed by the freeze)

Applied after the initial batch, recorded in commit `280cf5e` before running:

- selection: story links mirror the page order top-to-bottom and each story
  shows its number (1., 2., 3.) — use the visible number;
- typing: the previous step must be a successful `click_target` on the field;
- finish: never guess; report only values actually seen.

## 4. Revision batch (prompt `m018t-v2`)

| task | outcome | details |
|---|---|---|
| 02 | **`finished`** ✓ | one `click_target` to t9 = the rank-3 link → `/story/d03/` (class fixed) |
| 11 | `blocked:recovery_exhausted` | `refused_focus` ×3 — same class **persisted despite the hard rule** |
| 41 | **`finished`** ✓ | same path as initial (2 actions) |
| 43 | **`finished`** ✓ | same path as initial (1 action) |
| 45 | **`finished`** ✓ | same path as initial (1 action) |

**4/5 productive.** Per plan §8, one revision was allowed and used; the
persisting class (type-before-focus, a 4B planning limit seen since M018C)
stops iteration — no further prompt tuning.

Total model usage: 17 calls over both batches (budget: ≤125 + ≤125).

## 5. Safety (all runs)

- Zero forbidden executed actions; zero password attempts executed.
- Refusals exercised live: `refused_target_stale`, `refused_target_hidden`,
  `refused_target_moved` (scripted smoke; plus `refused_focus` passthrough in
  model runs). `refused_target_disabled` and `refused_target_offscreen` are
  **not constructible against the frozen fixture content** (no disabled
  controls exist in the site; no page overflows the viewport) — recorded gap;
  both are covered by deterministic tests and source-pinned helper logic.
- 41/45 oracles verified their constraints (correct destination, zero decoy
  activations); remote layout task 43 completed after the page settled.

## 6. §9 tally (clause by clause, as measured)

1. **Deterministic: MET** — M018T sweep 113 green; `browser_cli verify` ok
   with manifest sha `df630b21…` unchanged; scripted smoke PASS end-to-end
   with zero orphans and cleanup.
2. **Safety: MET with a recorded coverage gap** — zero forbidden actions and
   zero password attempts across every run; live refusal demonstration 3/5
   codes (gap above: disabled/offscreen not constructible; deterministic
   coverage exists).
3. **Signal: first clause MET** (4/5 revision batch, all genuine) **; the
   43/45 refusal clause was not triggered as-run** — in both batches the
   observation landed after the page settled, so the correct outcome was a
   normal click (no refusal warranted); the moved-refusal + re-observe path
   was exercised live in the scripted smoke on the same page instead.

No further tuning is permitted by the freeze; the treatment is measured at
smoke scale and the record stands regardless of direction.

## 7. Next step (investment decision, checkpoint 3)

Per plan §9, proceeding means building a **fresh evaluation set** (new
fixture instance content, new task ids, positives + mutations proven before
any run, ≈20 tasks, evaluated once each) **with its own recorded freeze and
review first**. The old held-out set cannot be reused. Recommendation: draft
that freeze for review; the smoke signal — 4/5 on tasks that all failed the
baseline, with zero safety regressions — is the evidence that makes the
investment worth considering, while task 11's persistent class is the known
limitation to carry into the evaluation honestly.

## Reproduction

```
PYTHONPATH=src .venv/bin/python -m vision_assistant.browser_cli target-smoke
PYTHONPATH=src .venv/bin/python -m vision_assistant.browser_cli task --mode target --ids 02,11,41,43,45
```
