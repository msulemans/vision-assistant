# M020 Stage 2 — schema-acceptance probe + no-model scripted gate (2026-09-20)

Status: **Stage 2 executed; Stage 3 NOT started (requires explicit go-ahead).**
Limits honored: no full test-suite run, no M019D/frozen/held-out tasks, no
Hacker News, no fresh evaluation tasks, no prompt changes, no model downloads.

## 1. Bounded probe — PASS (one model call)

`scripts/m020_stage2_probe.py` — one attempt only, no retries. It drives the
REAL typed pipeline once on the pinned model: dev fixture `/prefs/`, live
observation (11 targets), the real `m019c-v2` prompt, the exact form-mode
schema, `model_proposer(..., require_schema=True)` (a rejected schema request
is fatal; no unconstrained fallback), then validates the reply with
`validate_typed_action`.

- model calls: **1 attempt / 1 generation** (the schema-constrained request
  was accepted — no 400 from the pinned llama-server build 10621).
- `schema_events`: `["schema"]`; fallbacks: **0**.
- reply: `{"action": "fill_field", "observation_id": "obs-1",
  "target_ref": "ui:5", "text": "hello"}` — keys are exactly the `fill_field`
  branch; `branch_matches = 1`; `cross_talk_fields = []`.
- validator: `ok` (valid against the live target list); orphans: 0.
- Artifact: `docs/evidence/m020-stage2/probe.json`.

Reading: the pinned runtime accepts `oneOf` + `additionalProperties:false`,
and the decoder emitted **exactly one branch with zero cross-talk fields** —
the M019D form-lane failure class is not expressible in this reply.

Erratum recorded: the first launch attempt died at import time (the script
wrote `from vision_assistant import fixtures`; the module is
`browser_fixtures`). No model call occurred in that attempt; fixed before the
single probe run.

## 2. No-model scripted gate — 5/6 in BOTH runs (known timing race)

`browser_cli m019-scripted` run twice (re-ran once for stability, matching the
M019B gate practice; both runs preserved: `scripted-run1.json`,
`scripted-run2.json`). Five scenarios PASS in both runs (answer, navigate,
form, credential-refusal, external-submit-refusal — all checks Y; zero
forbidden actions, zero submissions, orphans 0). `moving-target` fails ONLY
its `moved_probe` check, identically in both runs: expected
`refused_target_moved`, got `"none"` (the click was not refused — no movement
was detected between the probe's target listing and its click).

Attribution: the probe waits a FIXED 1.6 s after listing, while the fixture
layout page shifts via `setTimeout(…, 1000)`
(`browser_fixtures.render_layout_page`) — the pass window requires the 1 s
shift to land between the listing and the click. Under today's load the
window slipped in both runs. This is the long-known fixture-timing race
(M019B lesson 3), in trusted probe code that **M020A never touched** (probe
mechanics unchanged since M019B, where the same gate passed twice); no M020A
code path is involved. No regression signal.

## 3. Focused tests — 125 OK

`test_m020_schema test_m019_invariants test_browser_agent test_browser_session
test_browser_tasks test_browser_typed` → **Ran 125 tests … OK**.

## Verdicts

- Stage 2 schema/runtime objective: **PASS**.
- Scripted gate: **5/6 ×2** — `moved_probe` timing race; recommend a tiny
  hardening (wait for the shift, or re-list and verify movement before
  clicking) or explicitly marking the probe best-effort before it is used as
  a gate again.
- Stage 3 (five fresh dev tasks): **eligible, NOT started** — needs explicit
  go-ahead.
